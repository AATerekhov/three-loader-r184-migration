export type PotreeDefaultAttributeValues = {
  color?: [number, number, number];
  uv?: [number, number];
  uv1?: [number, number];

  normal?: [number, number, number];
  classification?: [number, number, number];
  indices?: [number, number, number, number];
};
